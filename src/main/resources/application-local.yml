

server :
  port : 5026

spring:
  rabbitmq:
    addresses: localhost
  elasticsearch:
    uris:
      - http://localhost:9200
  ## to fill if authentication is needed
  # username:
  # password:

powsybl-ws:
  database:
    host: localhost

gridsuite:
  services:
    study-server:
      base-uri: http://localhost:5001
