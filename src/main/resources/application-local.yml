server :
  port : 5026

spring:
  rabbitmq:
    addresses: localhost
  data:
    elasticsearch:
      host: localhost
      ## to fill if authentication is needed
      # user:
      # password:

powsybl-ws:
  database:
    host: localhost

gridsuite:
  services:
    study-server:
      base-uri: http://localhost:5001
